<%= form_with(model: state, local: true) do |form| %>
  <% if state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state, id: :state_state %>
  </div>

  <div class="field">
    <%= form.label :capital %>
    <%= form.text_field :capital, id: :state_capital %>
  </div>

  <div class="field">
    <%= form.label :population %>
    <%= form.number_field :population, id: :state_population %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
